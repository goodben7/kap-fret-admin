[build]
  command = "npm run build"
  publish = "dist"

# SPA React Router — toutes les routes renvoient index.html
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[build.environment]
  NODE_VERSION = "22"
